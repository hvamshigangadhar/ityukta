import React from "react";
import { CssBaseline, Typography } from "@mui/material";

const Nontechnical = () => {
  return (
    <CssBaseline>
      <Typography variant="h1">Nontechnical</Typography>
    </CssBaseline>
  );
};

export default Nontechnical;
