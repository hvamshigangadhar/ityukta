import React from "react";
import { CssBaseline, Typography } from "@mui/material";

const Workshop = () => {
  return (
    <CssBaseline>
      <Typography variant="h1">Workshop</Typography>
    </CssBaseline>
  );
};

export default Workshop;
